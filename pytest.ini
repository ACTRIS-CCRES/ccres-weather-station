[pytest]

addopts =
    -vv
    --cov-config=.coveragerc
    --cov-report html:coverage/html
    --cov-report xml:coverage/coverage.xml
    --cov-report term
    --cov=.
testpaths = ccres_weather_station/tests
filterwarnings =
    ignore::DeprecationWarning
    default:::ccres_weather_station.*
